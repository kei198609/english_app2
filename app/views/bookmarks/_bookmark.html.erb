

    <% if @post.bookmarked_by?(current_user) %>
      <td><%= link_to "ブックマークを外す", post_bookmark_path(@post), data: { turbo_method: :delete } %></td>
    <% else %>
      <td><%= link_to "ブックマーク", post_bookmarks_path(@post), data: { turbo_method: :post } %></td>
    <% end %>


