<div class="container">
  <div class="row">
    <div class="col-md-4 mt-3">
      <section>
        <%= render 'users/registrations/link_list' %>
      </section>
    </div>
    <div class="col-md-8 bg-white mt-3 px-4">
      <div class="profile d-flex justify-content-center mt-4">
        <%= t('.delete_account') %>
      </div>
      <div class="mt-3">
          <p>一度アカウントを削除すると、二度と元に戻せません。十分ご注意ください。</p>
      </div>
      <div class="mb-4">
        <%= button_to t('.cancel_my_account'),
                      registration_path(resource_name),
                      data: { turbo_confirm: t('.are_you_sure') },
                      method: :delete, class: "btn btn-danger text-white" %>
      </div>
    </div>
  </div>
</div>